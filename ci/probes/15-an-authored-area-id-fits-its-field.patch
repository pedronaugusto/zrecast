# expect: area authoring Recast could not read is refused before anything is baked
--- a/ffi/zrecast_internal.h
+++ b/ffi/zrecast_internal.h
@@ -394,7 +394,7 @@
         v.shape != ZRC_VOLUME_CYLINDER) {
       return ZRC_ERR_INVALID_ARGUMENT;
     }
-    if (v.area < 0 || v.area >= ZRC_MAX_AREAS) return ZRC_ERR_INVALID_ARGUMENT;
+    if (v.area < 0) return ZRC_ERR_INVALID_ARGUMENT;
     if (!IsFinite(v.y_min) || !IsFinite(v.y_max) || v.y_min > v.y_max) {
       return ZRC_ERR_INVALID_ARGUMENT;
     }
